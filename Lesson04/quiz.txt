(1) The first index of all arrays in c++ is 0. Since the array is of length 5, the last index is 4. 

(2) No, I would never us a c-style string when c++ has strings types.

(3) '\0', the null-terminator, is one character as seen by the compiler.

(4) If you forget '\0' in a c-style string the program can crash.

(5) vector <char> question5 (5) ;


